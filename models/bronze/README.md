# Bronze Layer
This directory logically represents the Bronze (Raw) layer. 
In this implementation, these tables are managed directly in the SQLite database `data/humanitarian.db` via `pipeline/ingest.py`.
