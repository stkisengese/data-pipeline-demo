# Gold Layer
This directory logically represents the Gold (Curated/Analytical Marts) layer.
In this implementation, these tables are managed directly in the SQLite database `data/humanitarian.db` via `pipeline/transform.py`.
